CURRENT_STATE=STATE_IN_PLAY
DEF STATE_MACHINE_UPDATE(DELTA)
    IF CURRENT_STATE=STATE_IN_PLAY THEN
TRACE()
        CURRENT_STATE=IN_PLAY_UPDATE(DELTA)
    ELSE
        RAISE("INVALID STATE!")
    ENDIF
ENDDEF
